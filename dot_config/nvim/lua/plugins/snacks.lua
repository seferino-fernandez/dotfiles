return {
  "folke/snacks.nvim",
  opts = {
    image = {
      enabled = true,
    },
    picker = {
      hidden = true, -- for hidden files
      ignored = true, -- for .gitignore files
    },
  },
}
